# MQTT Logger to CSV file
Log the payload and topic from a Broker, and saves it into a csv file.
You can run it in backround on Linux with the following command:  
 nohup python3 MQTT_Logger_to_CSV_APP.py &
